# CodeWhale Configuration
# 示例：将本文件重命名为 config.toml 并放到 ~/.codewhale/ 目录下使用
# 或者直接使用 codewhale-tool 工具动态管理，无需手动编辑

[model]
active_provider = ""
active_api_key = ""
active_model = ""

[skills]
enabled = true