{
  "settings": {
    "security": "セキュリティと権限",
    "permissions.toml": {
      "label": "権限ルールファイル",
      "help": "tool + オプションの command/path フィールドをサポート。一致するルールは非 YOLO モードで強制承認をトリガーします。never モードでは一致すると直接拒否されます（ポップアップを表示できないため）。"
    }
  }
}
