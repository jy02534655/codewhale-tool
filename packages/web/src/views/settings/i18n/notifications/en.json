{
  "settings": {
    "notifications": "Notifications",
    "notifications.include_summary": {
      "label": "Include Summary",
      "help": "Defaults to false. When enabled, notifications will include elapsed time and estimated cost for the turn."
    },
    "notifications.sound_file": {
      "label": "Sound File",
      "help": "Only used when completion_sound is set to file. Custom WAV sound file path, Windows only."
    },
    "notifications_method": {
      "label": "Notification Method",
      "help": "Completion notification method. auto selects based on terminal, osc9 sends OSC 9, bel sends terminal bell, off disables notifications."
    },
    "notifications_threshold_secs": {
      "label": "Threshold",
      "help": "Only triggers notifications for successfully completed turns that take >= this value, default 30s. Failed/cancelled turns are always silent."
    },
    "notifications_completion_sound": {
      "label": "Completion Sound",
      "help": "Completion sound. beep uses the system default beep, off disables it, bell sends terminal bell, file plays a custom WAV."
    }
  }
}

